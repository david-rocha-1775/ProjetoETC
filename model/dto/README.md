# DTO (Data Transfer Object)

Esta pasta contém as classes que representam as entidades do sistema.

- **Responsabilidade:** Mapear as tabelas do banco de dados em objetos PHP.
- **O que colocar aqui:** Classes com atributos privados e métodos `getters` e `setters`.