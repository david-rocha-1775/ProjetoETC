# DAO (Data Access Object)

Aqui ficam as classes que executam as operações de banco de dados (CRUD).

- **O que colocar aqui:** Classes que contêm `INSERT`, `SELECT`, `UPDATE` e `DELETE`.
- **Arquivo `conexao.php`:** Gerencia a instância da conexão (PDO ou MySQLi).
teste